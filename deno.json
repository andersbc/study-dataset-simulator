{
  "workspace": [
    "./apps/api",
    "./packages/shared"
  ],
  "imports": {
    "@std/csv": "jsr:@std/csv@0.224.0",
    "hono": "npm:hono@4.1.0",
    "hono-rate-limiter": "npm:hono-rate-limiter@0.4.0",
    "dax": "jsr:@david/dax@0.44.1"
  },
  "tasks": {
    "dev:api": "deno task --cwd apps/api dev",
    "test:api": "deno task --cwd apps/api test",
    "sync:r": "deno run -A scripts/sync_r_logic.ts",
    "dev:start": "mkdir -p apps/api/logs data .deno_cache && docker compose -f docker-compose.dev.yml up --build",
    "start:api": "mkdir -p apps/api/logs data .deno_cache && deno task sync:r && docker compose -f docker-compose.dev.yml up --build api",
    "start:web": "docker compose -f docker-compose.dev.yml up --build web",
    "deploy": "deno run -A scripts/deploy.ts",
    "test": "deno task test:api"
  }
}